require 'rails_helper'

RSpec.describe DashboardController, type: :controller do

    # describe DashboardController do
    #     describe "status_options" do
    #         it "Gives Open, Captured,or Closed options to admin" do
    #             admin_user = User.new(role:"admin")
    #             expect(controller.status_options(admin_user)).to eq(['Open', 'Captured', 'Closed'])
    #         end
    #     end
    # end

end
